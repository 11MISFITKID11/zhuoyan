# 依赖与缓存
node_modules/
.npm-cache/

# IDE
.vscode/

# 环境变量（含密钥，绝不能提交）
.env
.env.*
!.env.example

# 运行数据目录（数据库 / JWT密钥 / API密钥 / 日志，均不入库）
data/
data.json
data.json.tmp
docx_raw.xml

# 日志与覆盖率
logs/
coverage/

# 拆分备份
index.html.bak

# 工作区本地数据（会话记忆等，不入库）
.workbuddy/
