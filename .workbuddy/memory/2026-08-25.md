# 2026-08-25 工作日志

## 前端工程化：index.html 拆分（P0）

### 背景
- 原 `index.html` 单文件 3809 行（HTML + CSS 730 行 + JS 2450 行 + SVG 图标 + emoji 脚本全塞一起），是最大的维护隐患。

### 拆分结果
- `index.html`：3809 → 613 行（只保留 HTML 结构 + SVG 图标精灵 + `<link>`/`<script>` 引入）
- `css/style.css`：739 行（主样式 + 图标尺寸）
- `js/app.js`：2409 行（主脚本，普通 `<script src>`，保持全局作用域，onclick 正常）
- `js/emoji-icons.js`：46 行（emoji→SVG 图标替换脚本）

### 方式
- 用 node 脚本按行号自动拆分，先备份 `index.html.bak`（3809 行，安全网保留）。
- 不引入构建工具、不改部署；Express `express.static(rootDir)` 直接服务 css/、js/ 子目录。

### 验证
- 静态文件 `index.html`/`css/style.css`/`js/app.js`/`js/emoji-icons.js` 全部 HTTP 200。
- 关键函数（checkLocalQuota/startAgentAnalysis/AIEngine/saveSettings 等）与关键 CSS 类均在，内容完整。

### 备注
- 项目 `index.html` 第 7 行已引入 mammoth CDN（docx 导入用）。
- `coverage/` 目录是 `jest --coverage` 生成，可加入 .gitignore。
- 遗留：`docx_raw.xml`（1.79MB）仍可清理。

## 审计修复三项（14:50）

### 1. 🔴 .env 密钥泄露
- `.gitignore` 补上 `.env` / `.env.*` / `!.env.example` / `logs/` / `coverage/` / `index.html.bak`。
- 新建 `.env.example`（占位符，不含真实密钥）。

### 2. 🟠 app.js 上帝对象拆分（2409 → 11 模块）
- `public/js/` 按功能域拆分：`api-client.js / state.js / utils.js / ai-engine.js / docs.js / auth.js / polish.js / logic.js / aigc.js / agent.js / main.js`。
- 普通 `<script src>` 保持全局作用域（onclick 内联可用）；加载顺序 = 基础→功能→初始化。
- `index.html` 按依赖顺序引入 11 个模块 + emoji-icons.js。

### 3. 🟠 public/ 隔离 + SW/Dockerfile 修复
- 前端整体移入 `public/`（index.html / css / js / manifest.json / sw.js / favicon.svg）。
- `config.js` 加 `publicDir`；`app.js` 静态服务 + SPA 回退改指 `config.publicDir`。
- `sw.js`：CACHE_NAME v2→v3，缓存列表改为真实路径（`/css/style.css`、`/js/*.js`），移除不存在的 favicon.ico。
- `Dockerfile`：删掉无效的 `COPY index.html`（已在 public/），密钥/数据库不打包进镜像（生产用环境变量 JWT_SECRET）；新建 `.dockerignore`。
- 补 `public/favicon.svg` + index.html `<link rel="icon">` + manifest icons 指向 svg。
- 验证：24 用例通过；public/ 资源全部 200。

### 遗留
- 根目录 `index.html.bak`（原始单文件备份，安全网，可留可删）。

## 修复：Word 文档导入（15:22）
- 根因 1：`mammoth` 走 `cdnjs.cloudflare.com` 外网 CDN，用户网络可能加载失败 → `mammoth` 未定义，`extractRawText` 报错。
- 根因 2（明确 bug）：`handleWordImport` 只处理 polish/logic/aigc 三个分支，`agent` 模块的 Word 导入按钮存在但点了无效果。
- 修复：① mammoth 下载到 `public/vendor/mammoth.browser.min.js`，`index.html` 改引用本地；② `public/js/utils.js` 的 `handleWordImport` 补 `agent` 分支（写入 `agentInput`）；③ 加 `mammoth` 未定义的降级提示。
- 验证：`/vendor/mammoth.browser.min.js` 200，页面正常。

## 加速：全文分析合并为「综合诊断」（15:30）
- `fullPaperAgent.js`：新增 `STEPS.diagnose`（综合诊断），把原 4 个独立步骤（术语/逻辑/风格/AIGC）合并为 **1 次 LLM 调用**（一次返回 4 项 JSON）。
- `_analyzeSingle`：步骤 2-5 并行 → 1 次 diagnose；`_analyzeChunked`：每块 1 次 diagnose 再 merge。
- 调用次数：8 次（含反思2）→ **5 次**（结构1+诊断1+报告1+反思2）；反思默认仍开（如关则 3 次）。
- 旧 STEPS（terms/logic/style/aigc）保留但不再被 analyze 调用（可后续清理）。
- 前端 `public/js/agent.js` 的 `AGENT_STEPS` 6 步 → 3 步（结构分析/综合诊断/综合报告）。
- 验证：mock 5 次调用跑通（4 项正确解析）；端到端 SSE 进度 `structure → diagnose → report`，overallScore 正常。

## 清理冗余文件（15:33）
- 删除 `docx_raw.xml`（1.79MB 测试残留）、`index.html.bak`（拆分备份，已稳定）、`coverage/`（jest 产物，可重新生成）。均确认无代码引用。
- 验证：服务正常 + 24 用例通过 + 资源 200，功能不受影响。

## P0 四项改造：流式 + 重试 + 取消 + token 计费（09:48，跨天续）
1. **流式正文输出**：`llm.js` 支持 `stream:true`（`_readStream` 解析 SSE 增量）；`ai.js` 5 个接口全部支持 `Accept: text/event-stream`（delta/complete/error 事件）；前端 `ai-engine.js` 新增 `streamCall`，AIEngine 5 方法支持 `{signal, onDelta}`；`logic.js`/`aigc.js` 文本类功能加打字机流式显示。
2. **JSON 解析加固**：`ai.js` 新增 `llmJsonCall`（`response_format:{type:'json_object'}` + 解析失败带错重试一次）；`parseJsonArray` 失败返回 null。
3. **可取消 + 重试**：后端 `makeAbortSignal`（req close → abort，刷新/取消不再白跑）；`llm.js` 对 429/5xx 指数退避重试 2 次（500ms/1s）；前端 AIEngine 传 signal/onDelta。
4. **token 计费**：`db.js` 新增 `llm_calls` 表 + `recordLlmCall`；`llm.js` 每次调用记录 tokens/耗时/成败；`consumeQuota` 改按 `usage.total_tokens` 扣；Agent 累计 `totalTokens`（含反思循环）并返回。
- 顺手修复：`apiSettings.model` 默认 ''（不再污染 qwen，后端 autoDetect）；`saveSettings` 不强制默认模型。
- 验证：SSE 润色 70 个 delta 事件；非流式真实 token 227 记录；全文分析 totalTokens=10157（含反思）；24 用例通过；服务正常。
